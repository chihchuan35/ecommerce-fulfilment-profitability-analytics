# SQL Scripts

This folder stores SQL scripts for the Microsoft Fabric Warehouse transformation layer.

The SQL workflow follows a simple ELT structure:

```text
Staging → Silver → Gold → Data Quality Checks
```
